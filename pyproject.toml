[project]
name = "paper-forge"
version = "0.3.0"
description = "A single personal-first Codex Skill for traceable academic paper reading."
readme = "README.md"
requires-python = ">=3.9"
dependencies = []

[tool.unittest]
start-dir = "tests"
