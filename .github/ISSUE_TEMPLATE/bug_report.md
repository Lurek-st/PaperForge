---
name: Bug report
about: Report a problem with PaperForge
title: "[bug] "
labels: bug
---

**Do not include** private PDFs, API keys, tokens, personal Zotero database files, your real `~/.paper-forge/profile.md`, or your real Obsidian Vault output in this report. The maintainer can never read those, and they will be redacted.

## Environment

- OS and version (e.g. Windows 11 23H2, macOS 14.5, Ubuntu 24.04):
- Python version (`python --version`):
- Zotero version (if relevant):
- Zotero Local API state (`paperforge.py doctor` output, sanitized):
- PaperForge version or commit (`git rev-parse HEAD`):

## Command

The exact PaperForge command you ran:

```bash
# paste the exact command here
```

## Expected behavior

What you expected to happen.

## Actual behavior

What actually happened, including any error output.

```text
# paste the relevant output here
```

## Reproduction steps

1.
2.
3.

## Sanitized logs

Attach or paste only sanitized logs. Remove any paths that point to private PDFs, your real Profile, or your real Vault.

## Anything else

Anything else that might help reproduce or diagnose the issue (related issues, prior attempts, workaround you tried, etc.).
